{
	"name": "yahtze",
	"displayName": "ZIP/TAR Explorer",
	"description": "Custom editor and explorer for ZIP, TAR, and TGZ archives or folders.",
	"keywords": [
		"zip",
		"tar",
		"tgz",
		"archives",
		"explorer"
	],
	"version": "0.1.0",
	"icon": "assets/icon.png",
	"publisher": "isopodlabs",
	"repository": "https://github.com/adrianstephens/vscode-explorer",
	"files": [
		"node_modules",
		"out/**/*.js",
		"assets/*",
		"LICENSE.txt",
		"!**/*.psd",
		"!**/*.gif",
		"!**/*.d.ts"
	],
	"engines": {
		"vscode": "^1.75.0"
	},
	"categories": [
		"Other",
		"Visualization"
	],
	"extensionKind": [
		"workspace"
	],
	"activationEvents": [
		"onFileSystem:zip"
	],
	"main": "./out/src/extension.js",
	"contributes": {
		"customEditors": [
			{
				"viewType": "zip.view",
				"displayName": "ZIP Viewer",
				"priority": "default",
				"selector": [
					{
						"filenamePattern": "*.zip"
					},
					{
						"filenamePattern": "*.jar"
					},
					{
						"filenamePattern": "*.apk"
					},
					{
						"filenamePattern": "*.vsix"
					},
					{
						"filenamePattern": "*.tar"
					},
					{
						"filenamePattern": "*.tgz"
					},
					{
						"filenamePattern": "*.tar.gz"
					}
				]
			}
		],
		"commands": [
			{
				"command": "zip.delete",
				"title": "Delete",
				"enablement": "!zip.readonly"
			},
			{
				"command": "zip.rename",
				"title": "Rename...",
				"enablement": "!zip.readonly"
			},
			{
				"command": "zip.explore",
				"title": "Reveal in ZIP Explorer..."
			}
		],
		"keybindings": [
			{
				"command": "zip.rename",
				"key": "f2",
				"when": "activeCustomEditorId == zip.view && !zip.readonly"
			},
			{
				"command": "zip.delete",
				"key": "del",
				"when": "activeCustomEditorId == zip.view && !zip.readonly"
			}
		],
		"menus": {
			"webview/context": [
				{
					"command": "zip.rename",
					"when": "webviewId == 'zip.view' && entry"
				},
				{
					"command": "zip.delete",
					"when": "webviewId == 'zip.view' && entry"
				}
			],
			"explorer/context": [
				{
					"command": "zip.explore",
					"when": "explorerResourceIsFolder"
				}
			]
		}
	},
	"scripts": {
		"build": "tsc -b",
		"watch": "tsc -watch -p ./",
		"watch-link": "tsc -b -watch ./tsconfig.link.json",
		"link": "npm link @isopodlabs/binary @isopodlabs/binary_libs @isopodlabs/utilities @isopodlabs/vscode_utils"
	},
	"devDependencies": {
		"@types/node": "^18",
		"@types/vscode": "^1.75.0",
		"typescript": "^5.8.0"
	},
	"dependencies": {
		"@isopodlabs/binary": "^1.8.1",
		"@isopodlabs/binary_libs": "^1.5.1",
		"@isopodlabs/utilities": "^1.9.0",
		"@isopodlabs/vscode_utils": "^0.5.0"
	}
}
